import { useState } from "react"
import { useNavigate } from "react-router-dom"

export const EmployeeUpdate = () => {

    const [employees, setEmployees] = useState([])
    const navigate = useNavigate()


    return <>Hellow EmployeeUpdate Worldie</>
}